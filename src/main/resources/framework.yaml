gamification:
  enabled: true
  events:
    - "grades"
    - "check-in"

payment:
  enabled-methods:
    - "PIX"
    - "CREDIT_CARD"
    - "DEBIT_CARD"
    - "BOLETO"

import:
    enabled-methods:
        - "JSON"
        - "CSV"